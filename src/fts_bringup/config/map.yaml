# Stub map config for nav2_map_server.
# Update 'image' to point at your actual map (pgm/png) before using Nav2.
image: map.pgm
resolution: 0.05
origin: [0.0, 0.0, 0.0]
occupied_thresh: 0.65
free_thresh: 0.196
negate: 0


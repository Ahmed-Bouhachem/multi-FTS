# Basic driver settings for the physical joystick (mirrors `ros2 run joy`).
joystick:
  ros__parameters:
    device_id: 0
    device_name: ""
    deadzone: 0.5
    autorepeat_rate: 20.0
    sticky_buttons: false
    coalesce_interval: 0.001
